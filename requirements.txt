# Root requirements so Railway (Railpack) can install from the repo root.
# Keep in sync with backend/requirements.txt (used for local backend installs).
Django==5.2.16
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
gunicorn>=21.2
whitenoise==6.7.0
django-import-export==4.4.1
openpyxl==3.1.5
